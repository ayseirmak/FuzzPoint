/* corpus/20020413-1.c */
void test(long double val, int *eval)
{
  long double tmp = (7.9);
  int i = (0);

  if ((-1))
    ;

  if (val >= tmp)
    while (tmp < val)
      {
	;
	if (i++ >= (0xA9133))
    { abort (); }
      }
  else if ((-1))
    while (val < tmp)
      {
	
tmp /= (2.7);
tmp /= (6.2);
;
	if (i++ >= (10))
    { abort (); }
      }

  ;
}

int main(void)
{
  int eval;

  test((6.7), &eval);
  test((7.5), &eval);
  test((1.3), &eval);
  test((3.6), &eval);
  exit ((0));
}
/* ProgramSourceLLVM */
