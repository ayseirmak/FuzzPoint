/* corpus/20000605-1.c */
typedef struct _RenderInfo RenderInfo;
struct _RenderInfo
{
    int y;
    float scaley;
    int src_y;
};

static void bar(void) { }

static int
render_image_rgb_a (RenderInfo * info)
{
  int y, ye;
  float error;
  float step;

  y = info->y;
  256;

  
;error = y * step;
1.0 / info->scaley;

  error = y * step;
  
;info->src_y += (int) error;
((int) error) - step;

  
int loop_break_1278 = 0;

int loop_break_1344 = 0;
for (; y < ye; y++) {
      

loop_break_1344++;
if(loop_break_1344<=11){
continue;
}

loop_break_1278++;
if(loop_break_1278<=8){
break;
}

if (error >= 1.0) {
	  info->src_y += (int) error;
	  (int) error;
	  bar();
      }
      step;
  }
  return info->src_y;
}

int main (void)
{
    RenderInfo info;

    0;
    0;
    1.0;

    if (0)
    { abort (); }
    exit(0);
}
/* ProgramSourceLLVM */
