/* corpus/20000605-1.c */
typedef struct _RenderInfo RenderInfo;
struct _RenderInfo
{
    int y;
    float scaley;
    int src_y;
};

static void bar(void) { }

static int
render_image_rgb_a (RenderInfo * info)
{
  int y, ye;
  float error;
  float step;

  info->y;
  
;1.0 / info->scaley;
256;

  
;y * step;

;error -= ((int) error) - step;
1.0 / info->scaley;

  
;
;error = y * step + 42 - ((short)(((int)(step)) << ((int)(y * step)))) * ((int)(((int)(error)) & ((int)(y * step)))) - ((int)(((double)(error)) + ((double)(error))));
error -= ((int) error) - step + 42 * ((short)(((double)(error)) * ((double)(step))));
error = y * step + 42 * ((long)(((int)(y)) & ((int)(y * step)))) + ((long)(((int)(y * step)) ^ ((int)(y * step)))) * ((int)(((double)(step)) - ((double)(error)))) + ((int)(((int)(step)) % ((int)(y * step))));
  
;(int) error;
((int) error) - step;

  
int loop_break_1410 = 0;
for (; y < ye; y++) {
      
loop_break_1410++;
if(0){
break;
}

if (0) {
	  (int) error;
	  (int) error;
	  bar();
      }
      step;
  }
  return info->src_y;
}

int main (void)
{
    RenderInfo info;

    
;0;
0;
    
;
;info.src_y = 0;
info.scaley;

;info.scaley = 1.0 + 42 - ((short)(((double)(info.scaley)) * ((double)(info.scaley)))) * ((int)(((double)(info.scaley)) + ((double)(info.scaley)))) * ((short)(((int)(info.scaley)) >> ((int)(1.0))));
info.src_y = 0;
    info.scaley = 1.0 + 42 * ((short)(((double)(info.scaley)) + ((double)(1.0))));

    if (0)
    { abort (); }
    exit(0);
}
/* ProgramSourceLLVM */
