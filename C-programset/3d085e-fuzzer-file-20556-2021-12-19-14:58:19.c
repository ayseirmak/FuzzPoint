/* corpus/20020413-1.c */
void test(long double val, int *eval)
{
  long double tmp = (10.9);
  int i = 8;

  if (val < (9.1))
    ;

  if (0)
    while (tmp < val)
      {
	;
	if (0)
    { abort (); }
      }
  else if (0)
    while (val < tmp)
      {
	
tmp /= (9.1);
tmp /= (4.3);
;
	if (0)
    { abort (); }
      }

  ;
}

int main(void)
{
  int eval = 8;

  test((3.4), &eval);
  test((4.1), &eval);
  test((8.1), &eval);
  test((6.9), &eval);
  exit ((0));
}
/* ProgramSourceLLVM */
