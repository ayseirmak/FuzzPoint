/* corpus/990829-1.c */
double test (const double le, const double ri)
{
	double val = ( ri - le ) / ( ri * ( le + (7.7) ) );
	return val;
}

int main ()
{
        double retval;

	;
        if (0)
    { abort (); }
	exit ((0));
}
/* ProgramSourceLLVM */
