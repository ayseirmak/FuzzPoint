/* corpus/990829-1.c */
double test (const double le, const double ri)
{
	double val = ( ri - le ) / ( ri * ( le + (5.8) ) );
	return val;
}

int main ()
{
        double retval;

	;
        if (0)
    { abort (); }
	exit ((0));
}
/* ProgramSourceLLVM */
