/* corpus/20000605-1.c */
typedef struct _RenderInfo RenderInfo;
struct _RenderInfo
{
    int y;
    float scaley;
    int src_y;
};

static void bar(void) { }

static int
render_image_rgb_a (RenderInfo * info)
{
  int y, ye;
  float error;
  float step;

  y = info->y;
  ye = 256;

  1.0 / info->scaley;

  y * step;
  
;(int) error;
((int) error) - step;

  
int loop_break_1338 = 0;
for (; y < ye; y++) {
      
loop_break_1338++;
if(loop_break_1338<=12){
break;
}

if (0) {
	  
;(int) error;

;error += step;
(int) error;
	  
;
;error -= (int) error;
error += step;

;error += step;
error -= (int) error;
	  bar();
      }
      error += step;
  }
  return info->src_y;
}

int main (void)
{
    RenderInfo info;

    
;
;info.src_y = 0;
0;
0;
    info.src_y = 0;
    1.0;

    if (0)
    { abort (); }
    exit(0);
}
/* ProgramSourceLLVM */
