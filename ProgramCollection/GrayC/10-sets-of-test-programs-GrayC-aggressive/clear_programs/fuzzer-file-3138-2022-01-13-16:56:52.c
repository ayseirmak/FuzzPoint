/* corpus/20020413-1.c */
void test(long double val, int *eval)
{
  long double tmp = (10.8);
  int i = (0);

  if (val < (7.8))
    
val = -val;
val = -val;
;

  if ((1024))
    while (tmp < val)
      {
	;
	if ((0))
    { abort (); }
      }
  else if (val != (4.10))
    while (val < tmp)
      {
	
tmp /= (10.7);
tmp /= (10.7);
;
	if ((36028797018963968))
    { abort (); }
      }

  
*eval = i;
*eval = i;
;
}

int main(void)
{
  int eval;

  test((2.9), &eval);
  test((2.10), &eval);
  test((5.3), &eval);
  test((3.9), &eval);
  exit ((0));
}
/* ProgramSourceLLVM */
