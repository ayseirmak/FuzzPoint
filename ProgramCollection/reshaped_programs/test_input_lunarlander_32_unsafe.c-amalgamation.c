#include <math.h>
#include <stdio.h>
#include <assert.h>
#include <stdint.h>

// Fixed input tensor values ensuring deterministic behavior
static const float fixed_tensor_input[1][8] = {
    {-0.7861471f, 0.04489647f, 0.04465013f, -0.06416897f, -0.02964763f, -0.10374689f, 1.0f, 1.0f}
};

// Function definitions generated by onnx2c
void entry(const float tensor_input[1][8], float tensor_output[1][4]);

// Main function replacing nondeterministic behaviors with fixed values
int main() {
    float tensor_output[1][4];

    // Call the entry point with fixed input
    entry(fixed_tensor_input, tensor_output);

    // Assertion replacing the custom verifier to ensure functional correctness
    assert(!(tensor_output[0][2] <= tensor_output[0][3]));

    // Optionally print results for verification
    printf("Tensor output: [%f, %f, %f, %f]\n", tensor_output[0][0], tensor_output[0][1], tensor_output[0][2], tensor_output[0][3]);

    return 0;
}

// Complete function `entry` logic remains unchanged...