#include <stdio.h>

int main() {
    double x = 0.5;  // Initialize x to a fixed typical value for illustration
    int result;

    // Perform the comparison to emulate original functionality
    result = (x < 1.0);
    
    // Log the result
    printf("Result of the operation x < 1.0: %d\n", result);

    return 0;
}