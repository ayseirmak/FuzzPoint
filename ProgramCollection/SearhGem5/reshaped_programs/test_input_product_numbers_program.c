#include <stdio.h>

int main() {
    // Initialize variables with fixed values
    double num1 = 3.5; // Example fixed value
    double num2 = 4.2; // Example fixed value
    double product = num1 * num2;

    // Print the product
    printf("Product = %.2lf\n", product);
    
    return 0;
}