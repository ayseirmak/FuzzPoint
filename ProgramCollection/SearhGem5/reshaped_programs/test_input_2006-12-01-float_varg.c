#include <stdio.h>

int main() {
    // Initialize and declare float variables with example values
    float f1 = 1.0f;  // Initialized to a typical use case value
    float f2 = 2.3f;
    float f3 = -4.5f;
    float f4 = 8.96f;

    // Print the values using printf
    printf("foo %f %f %f %f\n", f1, f2, f3, f4);

    // Indicate successful execution
    return 0;
}